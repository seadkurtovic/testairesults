# testairesults
Human-in-the-loop validation tool for AI document extraction. Upload PDFs and structured AI outputs (JSON/CSV), review fields against the original document, and automatically compute precision, recall, and accuracy. Reproducible evaluation without Excel.
